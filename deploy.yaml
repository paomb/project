apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  namespace: nodejs-app
  labels:
    app: myapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: paomb/nodejs-app
        ports:
        - containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
  name: app-balancer
  namespace: nodejs-app
spec:
  type: LoadBalancer
  selector:
    app: myapp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080



